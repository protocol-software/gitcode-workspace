import { Route } from '@angular/router';
import {SearchCodeComponent} from './search-code.component';

export const SearchCodeRoutes: Route[] = [
    {
        path     : '',
        component: SearchCodeComponent,
    }
];
